import './App.css'

const App = () => {

  return (
    /**
     * Note that you have to wrap your jsx with a div of some sort,
     * the sample/conventional way is to use an empty wrapper (Fragment).
     * But doing the following will also work in the case you want to apply
     * some sort of styling to the entire component.
     * 
     * <div>
     *  <h1> Helllooooo </h1>
     * </div>
     */

    <>
      <h1>Hello world!</h1>
    </>
  )
}

export default App
